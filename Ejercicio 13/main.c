#include <stdio.h>
#include <stdlib.h>

int main()
{
    //13.Hacer el programa que imprima los números del 100 al 0 en orden decreciente

    for (int i = 100 ; i >= 0  ; i--)
    {
        printf("%d\n",i);
    }
}
